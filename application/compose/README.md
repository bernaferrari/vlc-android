# VLC Compose Module

**Isolated Compose entry point for the VLC Android hybrid migration.**

This is a standalone Android library module (`:application:compose`) that provides the foundation for introducing Jetpack Compose into the existing VLC for Android codebase without a big-bang rewrite.

## Why an Isolated Module?

- **Lowest-risk path**: New Compose code lives completely separately from legacy XML/DataBinding/fragment code.
- Permanent exceptions are expected and accepted (native C++ surfaces, complex legacy players, certain dialogs).
- Strategic goal: achieve **~80% migratable surface** over time. The remaining 20% stays in the classic architecture.
- Allows incremental adoption, easy rollback, and parallel development on the bootstrap branch.

## How to Consume

In any other module (most commonly `:application:app` or `:application:television`):

```groovy
// In the consuming module's build.gradle
dependencies {
    implementation project(':application:compose')
}
```

Then import:

```kotlin
import org.videolan.vlc.compose.theme.VLCTheme
import org.videolan.vlc.compose.components.VLCDropdownItem
import org.videolan.vlc.compose.interop.VLCComposeView
import org.videolan.vlc.compose.interop.VLCAbstractComposeWidget
```

## Interop Patterns (Hybrid Migration)

This module deliberately exposes several bridges so Compose can be used immediately inside the existing XML-heavy architecture.

### 1. VLCComposeView (for XML layouts)

Drop-in replacement for `ComposeView`. Use directly inside any legacy layout:

```xml
<org.videolan.vlc.compose.interop.VLCComposeView
    android:id="@+id/my_compose_host"
    android:layout_width="match_parent"
    android:layout_height="wrap_content" />
```

In Kotlin (including inside DataBinding fragments/activities):

```kotlin
binding.myComposeHost.setContent {
    VLCTheme {
        MyComposableScreen(...)
    }
}
```

### 2. VLCAbstractComposeWidget (for reusable custom views)

Subclass and implement `WidgetContent()` to create a self-contained Compose widget that can be inflated from XML exactly like any other custom view:

```kotlin
class MyComposeWidget @JvmOverloads constructor(
    context: Context,
    attrs: AttributeSet? = null,
    defStyleAttr: Int = 0
) : VLCAbstractComposeWidget(context, attrs, defStyleAttr) {

    @Composable
    override fun WidgetContent() {
        VLCTheme {
            Text("Hello from Compose widget")
        }
    }
}
```

Use `<your.package.MyComposeWidget ... />` in any layout.

### 3. Full Compose Screens / Destinations

For new screens or major refactors, host entire `@Composable` trees using `VLCTheme` as the root. These can live in Compose-only fragments or be launched via the navigation system once Compose Navigation is introduced.

```kotlin
@Composable
fun MyFullScreen() {
    VLCTheme {
        // Material3 Scaffold, Lists, etc.
    }
}
```

## Current Status

- Basic theming foundation (`VLCTheme`, placeholder semantic tokens mapped from future `colors.xml` + `styles.xml` audit). Tokens expanded with Wave 1 additions (headerBackground, audioBrowserSeparator, onboardingBackground).
- Interop helpers (`VLCComposeView`, `VLCAbstractComposeWidget`).
- Leaf Composables (Wave 1):
  - `VLCDropdownItem` (original)
  - `VLCSectionHeader` (hosted by `SectionHeaderDecorationView` for recycler section decorations)
  - `VLCInfoItem` (info_item.xml - high reuse in track info)
  - `VLCDebugLogLine` (debug_log_item.xml)
  - `VLCDialogConfirmDelete` (presentational parts of dialog_confirm_delete.xml)
  - `VLCOnboardingWelcome` (static parts of onboarding_welcome.xml)
  - `VLCBrowserItemRow` / `VLCBrowserItemCard` (Wave 2 media browser rows/cards from former browser/history/MRL row patterns)
  - `VLCEmptyState` (Wave 2 shared loading/empty surfaces for audio/video/browser/playlist screens)
  - `VLCAudioPlayerChips` (Wave 2 playback speed + sleep quick-action chips from AudioPlayerShell.kt)
  - `VLCVideoQuickActions` (Wave 2 video HUD quick-action chips now hosted by the Compose top/right HUD overlay)
  - `VLCAudioQueueProgressPill` (Wave 2 audio queue progress HUD chip from AudioPlayerShell.kt)
  - `VLCAudioMiniProgressBar` (Wave 2 collapsed audio-player mini playback progress bar from AudioPlayerShell.kt)
  - `VLCAudioPlayerBackground` (Wave 2 audio-player blurred cover background from AudioPlayerShell.kt)
  - `VLCAudioPlayerGradient` (Wave 2 audio-player top/bottom gradient overlays from AudioPlayerShell.kt)
  - `VLCAudioPlaylistItem` (Wave 2 audio/video playlist row)
  - `VLCAudioPlaylistSearchField` (Wave 2 audio-player playlist search input from AudioPlayerShell.kt)
  - `VLCAudioTimelineSlider` (Wave 2 audio/video full-player timeline seekbars from AudioPlayerShell.kt and VideoHudOverlayView)
  - `VLCAudioResumeVideoHint` (Wave 2 audio-player restore-video hint from AudioPlayer.onResume)
  - `VLCAudioHeaderBackground` / `VLCAudioHeaderDivider` (Wave 2 collapsed audio-player header decorative surfaces from AudioPlayerShell.kt)
  - `VLCAudioHeaderTimeLabel` (Wave 2 collapsed audio-player header time label from AudioPlayerShell.kt)
  - `VLCAudioHeaderActionButton` (Wave 2 collapsed audio-player search/playlist/overflow + AB-repeat reset/stop actions from AudioPlayerShell.kt)
  - `VLCAudioHeaderPlayPauseButton` (Wave 2 collapsed audio-player mini play/pause control from AudioPlayerShell.kt)
  - `VLCAudioHeaderTransportButton` (Wave 2 audio-player header + full-player transport + landscape chapter + foldable hinge controls from AudioPlayerShell.kt)
  - `VLCAudioHeaderMediaSwitcher` (Wave 2 collapsed audio-player previous/current/next title/cover pager from AudioPlayerShell.kt)
  - `VLCAudioCoverMediaSwitcher` (Wave 2 full-cover audio-player previous/current/next artwork/title pager from AudioPlayerShell.kt)
  - `VLCAudioSeekHudButton` / `VLCAudioSeekDelayLabel` (Wave 2 audio-player cover-mode seek/bookmark HUD controls from AudioPlayerShell.kt)
  - `VLCPlayerOptionItem` (Wave 2 shared player options row from player_option_item.xml)
  - `VLCBookmarkRow` (Wave 2 shared bookmark list row from bookmark_item.xml)
  - `VLCAbRepeatControls` (Wave 2 shared A-B repeat add-marker chip root now hosted directly in audio/video HUD layouts)
  - `VLCAbRepeatChipIcon` (Wave 2 shared A-B repeat add-marker chip icon now hosted directly in audio/video HUD layouts)
  - `VLCAbRepeatAddMarkerButton` (Wave 2 shared A-B repeat add-marker button now hosted directly in audio/video HUD layouts)
  - `VLCAudioAbRepeatMarkers` / `VLCAudioAbRepeatMarker` (Wave 2 audio/video A-B repeat timeline marker strip from AudioPlayerShell.kt + VideoHudOverlayView)
  - `VLCBookmarkMarkers` (Wave 2 audio/video bookmark timeline markers from AudioPlayerShell.kt + VideoHudOverlayView)
  - `VLCAudioTrackInfoText` (Wave 2 landscape audio-player title/subtitle/detail text from AudioPlayerShell.kt)
  - `VLCAudioTimelineTimeLabel` (Wave 2 audio-player elapsed/length labels from AudioPlayerShell.kt)
- Full Compose Activity screens:
  - `VLCOTPCodeScreen` hosted by `OTPCodeActivity` (former OTP XML layout removed)
  - `VLCBetaWelcomeScreen` hosted by `BetaWelcomeActivity` (former DataBinding XML layout removed)
  - `VLCAuthorsScreen` hosted by `AuthorsActivity` (former DataBinding RecyclerView/list XML removed)
  - `VLCLibrariesScreen` hosted by `LibrariesActivity` (former DataBinding RecyclerView/list XML removed; license detail is Compose in this screen)
  - `VLCAboutScreen` hosted by `AboutActivity` (former About XML and license/version bottom-sheet paths removed)
  - `VLCFeedbackScreen` hosted by `FeedbackActivity` (former DataBinding XML layout removed)
  - `RemoteAccessShareActivity` hosted by a Compose screen in `:application:remote-access-server` (former DataBinding screen XML, connection row XML, and toolbar menu XML removed)
  - `MediaScrapingActivity` hosted by a Compose screen in `:application:moviepedia` (former DataBinding screen XML, result row XML, and RecyclerView adapter removed)
  - TV `SearchActivity` hosted by `VLCSearchScreen` with native Compose thumbnail rendering for media search results (former result `AndroidView`/`ImageView` bridge removed)
  - TV `MediaScrapingTvActivity` hosted by `VLCSearchScreen` with native Compose remote poster rendering for moviepedia search results (former result `AndroidView`/`ImageView` bridge removed)
  - TV `MainTvActivity` hosted by a Compose home screen with native Compose poster and media-card artwork rendering (former home-card `AndroidView`/`ImageView` bridges removed)
  - TV `VerticalGridActivity`, `DetailsActivity`, `MediaScrapingTvshowDetailsActivity`, and `MediaListActivity` now render browser/detail artwork through native Compose `Image` helpers (remaining TV detail/browser `AndroidView`/`ImageView` artwork bridges removed)
- Compose-hosted root screens:
  - `PreferencesActivity` root settings list plus Interface, Video, Subtitles, Audio, Casting, Parental Control, Remote Access, Android Auto, Advanced, and Optional Features subpages (former XML preference subpages removed)
  - `VLCPinCodeScreen` hosted by `PinCodeActivity` (former DataBinding PIN keypad XML layout removed)
  - `VLCSearchScreen` hosted by `SearchActivity` with native Compose thumbnail rendering for phone media search results (former DataBinding Activity/result row XML layouts and result `AndroidView`/`ImageView` bridge removed)
  - `VLCEqualizerSettingsScreen` hosted by `EqualizerSettingsActivity` (former DataBinding Activity/list row XML layouts and menu XML removed)
  - `VLCEqualizerEditorDialogContent` hosted by a Compose player bottom sheet (former vertical slider views and dialog XML layouts removed)
  - `VLCWhatsNewDialogContent` hosted by a Compose bottom sheet from `WhatsNewManager` (former DataBinding XML removed)
- Compose player hosts:
  - `AudioPlayerContainerActivity` now instantiates `AudioPlayer` as a bottom-sheet controller inside a programmatic `AudioPlayerHostView` (former XML host path removed; bd compose-55n / compose-68e)
- App widget boundary (bd compose-l0l):
  - The widget configuration and migration UI is Compose-hosted: `MiniPlayerConfigureActivity`, `WidgetExplanationComposeDialog`, and `WidgetMigrationComposeDialog`.
  - The launcher-hosted App Widget runtime is intentionally scoped outside the normal Activity/screen Compose completion check because Android App Widgets are rendered by launcher hosts through `RemoteViews`, not by app-owned `ComposeView` screens. The active provider metadata remains `res/xml/widget_provider_mini.xml`, `res/xml/widget_provider_b.xml`, and `res/xml/widget_provider_w.xml`.
  - Active RemoteViews layout XML is limited to widget runtime files: `widget_pill.xml`, `widget_mini.xml`, `widget_mini_initial.xml`, `widget_micro.xml`, `widget_macro.xml`, `widget_content_full_player.xml`, and `widget_old.xml`. `MiniPlayerAppWidgetProvider` selects the pill/mini/micro/macro layouts through `WidgetType`; the old white/black providers use `widget_old.xml` only for migration/compatibility and are disabled by `WidgetMigration` once no installed old widgets remain.
  - Decision: do not block the "every screen full Compose / no Fragment" goal on these launcher-hosted RemoteViews, but do not claim they are Compose screens. A future Glance rewrite can be prototyped if product wants Compose-style widget source, with the explicit caveat that Glance still targets App Widget/RemoteViews hosting and requires a minSdk/dependency decision.
- Glance widget prototype (bd compose-0e9):
  - The app module now depends on `androidx.glance:glance-appwidget` through the existing root `build.gradle` version registry (`glanceVersion = '1.1.1'`). This repo does not currently use a Gradle version catalog, so no catalog file was introduced.
  - Glance 1.1.1 declares `minCompileSdk=34`; the current app compileSdk is 36 and the focused `:application:vlc-android:compileDebugKotlin` gate passes on the VLC 3 minSdk 17 build path.
  - `MiniPlayerGlanceWidget.kt` adds a compile-verified `GlanceAppWidget`/`GlanceAppWidgetReceiver` prototype and maps all active mini-player widget types: pill, mini, micro, and macro. It covers the core source-shape for title/artist text, cover/icon/progress bitmap slots, play/pause, previous/next, seek-forward/seek-backward, open-app click handling, widget colors, seek-delay state, and the XML-to-Glance runtime deletion target.
  - The current manifest intentionally still points at `MiniPlayerAppWidgetProvider`, because production replacement still needs Glance state persistence keyed by AppWidget id, provider metadata migration, configuration preview wiring, cover/progress bitmap invalidation parity, and legacy white/black widget compatibility. After that provider swap, `widget_pill.xml`, `widget_mini.xml`, `widget_micro.xml`, `widget_macro.xml`, and `widget_content_full_player.xml` become deletable. `widget_mini_initial.xml` and `widget_old.xml` remain until provider metadata and legacy migration are also converted or removed.
- All new leaves respect full VLCTheme tokens + typography, have rich light/dark @Previews, and full KDoc traceability to original XML paths.
- Preview support for Android Studio (see `PreviewUtils.kt`).
- Proper library packaging (consumer rules, proguard, test setup).

**Wave 1 Cross-cutting Milestone (compose-2l4.1.8)**: The dedicated **Compose Interop Lab** now exists as the crown jewel dev-only interactive showcase (see below). It hosts live examples of all six leaves + realistic combined usage (sectioned lists, dialog-in-context, live simulators). This advances both the "interop demo" and "preview + gate enforcement" acceptance criteria for the wave.

This module is intentionally minimal on purpose — it is the **bootstrap foundation**. Real component migration work happens in follow-up issues.

## Design Principles

- Everything public API is under `org.videolan.vlc.compose.*`
- Never enable ViewBinding or DataBinding inside this module.
- Use Material 3 exclusively.
- All Composables should accept `Modifier` and be previewable.
- Theme tokens will be expanded during the color/style audit (Phase 0 / follow-up work).

## Running / Validating

```bash
# From repo root (worktree)
gradle :application:compose:build --no-daemon --console=plain
gradle :application:compose:connectedCheck --no-daemon --console=plain   # requires device/emulator
```

Android Studio will immediately show `@Preview` renders for the theme and components once the module is synced.

## Wave 1 Interop Lab & Build Gate Policy (compose-2l4.1.8)

The **Compose Interop Lab** (`ComposeInteropLabActivity` + `compose_interop_lab.xml`) is the single most important visibility artifact delivered in Wave 1.

- **Launch**: Dev-only button added to `DebugLogActivity` (the primary developer entrypoint, itself reachable from Advanced Preferences). See `debug_log.xml` for the button + its documentation block.
- **What it contains**: A scrollable, interactive column exercising **all six Wave 0/1 leaves live**:
  - `VLCDropdownItem`, `VLCSectionHeader`, `VLCInfoItem`, `VLCDebugLogLine`, `VLCDialogConfirmDelete`, `VLCOnboardingWelcome`
  - Plus richer combined mocks (sectioned list using Header+InfoItem, live log simulator with mutable state, real `AlertDialog` wrapping the delete leaf, onboarding variants, etc.).
- **Pattern**: Full-screen demonstration of **Pattern 1** (VLCComposeView in legacy layout XML + `setContent { VLCTheme { ... } }`). The Activity is intentionally thin.
- **Educational quality**: Matches the exhaustive comment style of the reference hosts `NetworkServerDialog.kt` (first demo, compose-5qk) and `DebugLogActivity.kt` (first real Wave 1 host migration, compose-2l4.1.2 / bd compose-5wg). Every file contains rollback instructions, traceability, Permanent Exceptions notes, and cross-references.
- **Pointers to host files** (update this list as new hosts land):
  - Primary interop demo host: `application/vlc-android/src/org/videolan/vlc/gui/dialogs/NetworkServerDialog.kt` + `network_server_dialog.xml`
  - First real Wave 1 host (list + adapter): `application/vlc-android/src/org/videolan/vlc/gui/DebugLogActivity.kt` + `debug_log.xml` (also contains the Lab launch button)
  - Browser/list section hosts: Compose browser and media-list surfaces render section content directly in Compose (compose-2l4.1.4)
  - Info surfaces host: `application/vlc-android/src/org/videolan/vlc/gui/InfoActivity.kt` (VLCInfoItem track rows inside a full Compose media-info screen; compose-2l4.1.3 / bd compose-l94 plus compose-l12)
  - Section header hosts (compose-2l4.1.4 / bd compose-95d)
  - Dialog content host (ConfirmDeleteDialog - keep shell/swap content pattern): `application/vlc-android/src/org/videolan/vlc/gui/dialogs/ConfirmDeleteDialog.kt` (compose-2l4.1.5 / bd compose-j0e)
  - Onboarding first-run host (high-visibility welcome flow): Compose onboarding welcome content (compose-2l4.1.6 / bd compose-mdj) — real logo slot + land variant noted, layouts 100% preserved
  - Media/browser row hosts: `PlaylistScreen.kt`, `VideoScreen.kt`, `MainBrowserScreen.kt`, `HistoryScreenContent`, and `HeaderMediaListActivity.kt` (`VLCBrowserItemRow` / `VLCBrowserItemCard`; compose-q9r.1 / compose-q9r.6 / compose-q9r.7 / compose-mqk; active paths no longer inflate the corresponding browser, history, stream card, or album-track row XML layouts, and the remaining header-media track list is a Compose `LazyColumn`)
  - Shared empty/loading hosts: `AudioScreen.kt`, `VideoScreen.kt`, `MainBrowserScreen.kt`, and `PlaylistScreen.kt` (`VLCEmptyState`; compose-q9r.2; replaces duplicated private empty/loading composables and removes the unused legacy empty-loading XML view)
  - Audio/video playlist queue hosts: `application/vlc-android/src/org/videolan/vlc/gui/audio/AudioPlayer.kt` + `AudioPlayerShell.kt` + `AudioPlaylistQueue.kt` + `AudioPlaylistMediaItem.kt` + `AudioPlaylistTipsDelegate.kt` + `AudioPlaylistTipsView.kt` + `application/vlc-android/src/org/videolan/vlc/gui/video/VideoPlayerActivity.kt` + `VideoPlayerOverlayDelegate.kt` + `VideoPlayerShell.kt` (`VLCAudioPlaylistItem`; compose-68e / compose-7py; replaces the former shared `PlaylistAdapter` RecyclerView path, `playlist_item.xml` bridge, and `audio_playlist_tips.xml` ViewStub payload with Compose LazyColumn hosts, native Compose row artwork/actions, and direct `VLCComposeView` audio/playlist tips overlays)
  - Audio player chrome hosts: `application/vlc-android/src/org/videolan/vlc/gui/AudioPlayerContainerActivity.kt` + `application/vlc-android/src/org/videolan/vlc/gui/AudioPlayerHostView.kt` + `application/vlc-android/src/org/videolan/vlc/gui/audio/AudioPlayer.kt` + `AudioPlayerShell.kt` + `AudioPlayerViews.kt` + `AudioPlaylistTipsDelegate.kt` (`VLCAudioPlayerChips`, `VLCAudioQueueProgressPill`, `VLCAudioMiniProgressBar`, `VLCAudioPlayerBackground`, `VLCAudioPlayerGradient`, `VLCAudioPlaylistSearchField`, `VLCAudioTimelineSlider`, `VLCAudioResumeVideoHint`, `VLCAudioHeaderBackground`, `VLCAudioHeaderDivider`, `VLCAudioHeaderTimeLabel`, `VLCAudioHeaderActionButton`, `VLCAudioHeaderPlayPauseButton`, `VLCAudioHeaderTransportButton`, `VLCAudioHeaderMediaSwitcher`, `VLCAudioCoverMediaSwitcher`, `VLCAudioSeekHudButton`, `VLCAudioSeekDelayLabel`, `VLCAbRepeatControls`, `VLCAudioAbRepeatMarkers`, `VLCBookmarkMarkers`, `VLCBookmarksPanelContent`, `VLCPlayerOptionsPanelContent`, `VLCAudioTrackInfoText`, `VLCAudioTimelineTimeLabel`; compose-q9r.3 / compose-68e / compose-55n; includes the bottom-sheet controller shell plus the explicit audio-player view holder, direct collapsed header, full-cover media switcher, blurred cover background, gradient overlay, playlist search input, mini-progress, full-timeline, bookmark-marker, bookmark-panel, options-panel, and A-B repeat hosts, restore-video hint, foldable hinge affordances, direct landscape `track_info_container` Compose host for chapter/track-info/seek HUD controls, full-player timeline labels, removed `AudioPlayerBinding`/DataBinding wrappers, and direct options-panel host without the former `player_options_container` `FrameLayout` wrapper)
  - Phone shell scan progress host: `application/vlc-android/src/org/videolan/vlc/gui/PhoneActivityShell.kt` + `AudioPlayerContainerActivity.kt` + `ScanProgressView.kt` (`ScanProgressView`; compose-f5c; replaces the shared `scan_progress.xml` ViewStub payload with a direct Compose host while preserving media-library discovery text, determinate/indeterminate parsing progress, and CoordinatorLayout anchoring/margins)
  - Video player lightweight overlay hosts: `application/vlc-android/src/org/videolan/vlc/gui/video/VideoPlayerOverlayDelegate.kt` + `VideoPlayerShell.kt` + `VideoHudOverlayViews.kt` + `VideoStatsDelegate.kt` + `VideoTipsDelegate.kt` + `VideoDelayDelegate.kt` + `VideoTouchDelegate.kt` + `VideoHudOverlayView.kt` (`VideoInfoOverlayView`, `VideoTipsHostView`, `VideoVerticalProgressOverlayView`, `VideoDelayOverlayView`, `VideoSeekOverlayView`, `VideoHudRightOverlayView`, `VideoHudOverlayView`, `SwipeToUnlockView`, `VideoTimelineSeekBarView`, `VideoTimelineTimeLabelView`, `AbRepeatMarkerContainerView`, `VLCAudioAbRepeatMarkers`, `VideoHudIconButtonView`, `VideoHudSeekJumpLabelView`, `VideoStatsOverlayView`; compose-91m / compose-68e / compose-slm / compose-6o9 / compose-oxh / compose-f7r; replaces the transient info ViewStub XML, the video tips ViewStub payload, brightness/volume progress ViewStub payloads, the audio/subtitle delay settings ViewStub payload, the double-tap seek/fast-play ViewStub payload, the top/right HUD ViewStub/DataBinding payload, the remaining bottom HUD `ViewStubCompat` host, the swipe-to-unlock XML payload, the video timeline `AccessibleSeekBar`, the video elapsed/length `FocusableTextView` labels, A-B repeat marker guideline/DataBinding islands, video A-B repeat reset/stop/warning `ImageView` icon buttons, static video HUD close/tracks/previous/rewind/forward/next/resize/more action icon buttons, the orientation icon button, seek jump number labels, the video play/pause icon button, the stats panel `NestedScrollView`/dynamic grid/`PlotView`/`LegendView` XML island, the remaining `player_hud.xml` DataBinding variables/expressions, the generated `PlayerHudBinding` view holder, the `player_hud.xml` XML shell, and the final `player.xml` / `player_remote_control.xml` root shells)
  - Video player quick-action hosts: `application/vlc-android/src/org/videolan/vlc/gui/video/VideoPlayerOverlayDelegate.kt` + `VideoHudRightOverlayView.kt` (`VLCVideoQuickActions`; compose-8pj / compose-f7r; replaces the Material `HorizontalScrollView`/`ChipGroup` quick-action strip and now hosts it inside the full Compose top/right HUD while preserving orientation, speed, sleep, subtitle-delay, and audio-delay callbacks)
  - Shared playback bookmark hosts: `application/vlc-android/src/org/videolan/vlc/gui/helpers/BookmarkListDelegate.kt` + `AudioPlayerShell.kt` + `BookmarkMarkerContainerView.kt` + `BookmarksPanelView.kt` + `VideoHudOverlayView.kt` (`VLCBookmarkMarkers`, `VLCBookmarkRow`, `VLCBookmarksPanelContent`; compose-68e / compose-8gz; direct timeline marker and bookmark-panel hosts for AudioPlayer and TV AudioPlayerActivity, plus video HUD `BookmarkMarkerContainerView`/`BookmarksPanelView` bridge compatibility)
  - Shared playback A-B repeat root: `application/vlc-android/src/org/videolan/vlc/gui/view/AbRepeatControlsView.kt` + `AbRepeatMarkerContainerView.kt` + `AudioPlayerShell.kt` + `VideoHudOverlayView.kt` (`VLCAbRepeatControls`, `VLCAbRepeatChipIcon`, `VLCAbRepeatAddMarkerButton`, `VLCAudioAbRepeatMarkers`; compose-68e; direct A-B repeat controls/marker hosts for AudioPlayer, with video HUD wrapper compatibility and the former shared include XML removed)
  - Shared playback option panel: `application/vlc-android/src/org/videolan/vlc/gui/helpers/PlayerOptionsDelegate.kt` + `AudioPlayerShell.kt` + `PlayerOptionsPanelView.kt` + `VideoPlayerShell.kt` (`VLCPlayerOptionItem`, `VLCPlayerOptionsPanelContent`; compose-68e / compose-8gz; direct options-panel hosts for AudioPlayer and TV AudioPlayerActivity through `PlayerOptionsPanelHost`, with video/player remote `PlayerOptionsPanelView` bridge compatibility)
  - TV audio-player full Compose screen: `application/television/src/main/java/org/videolan/television/ui/audioplayer/AudioPlayerActivity.kt` + `TvAudioPlaylist.kt` + `TvAudioQuickActions.kt` + `TvAudioTransportControls.kt` + `TvAudioNowPlayingText.kt` + `TvAudioTimeline.kt` + `TvAudioBookmarkMarkers.kt` + `TvAudioBookmarksPanel.kt` + `TvAudioPlayerOptionsPanel.kt` + `TvAudioArtwork.kt` (`TvAudioPlayerScreen` / `TvAudioPlaylist` / `TvPlaylistRow` / `TvAudioQuickActions` / `TvAudioTransportControls` / `TvAudioTrackInfo` / `TvAudioProgressLabels` / `TvAudioTimeline` / `TvAudioBookmarkMarkers` / `TvAudioBookmarksPanel` / `TvAudioPlayerOptionsPanel` / `TvAudioBackground` / `TvAudioAlbumCover`; compose-st7 / compose-68e; replaces the `tv_playlist_item.xml` DataBinding row, stale TV bookmark/player-options ViewStub layout refs, the former `PlaylistAdapter` RecyclerView list, the playlist-row cover `ImageView` bridge, the TV speed/sleep `LinearLayout` quick-action chips, the TV shuffle/previous/play/next/repeat/more `ImageView` transport row, the TV title/artist/elapsed/length `TextView`s, the TV progress `AudioTimelineSeekBarView` wrapper, the TV bookmark-marker `BookmarkMarkerContainerView` wrapper, the TV bookmark-panel `BookmarksPanelView` wrapper, the TV option-panel `PlayerOptionsPanelView` wrapper, the TV album/background `ImageView`s, and the TV audio-player root `AndroidView`/programmatic `ConstraintLayout` shell with a single Compose screen body)
  - Preferences root/subpage host: `application/vlc-android/src/org/videolan/vlc/gui/preferences/PreferencesActivity.kt` + `PreferencesRootScreen.kt` + `PreferencesComposeSubpages.kt` (bd compose-qeh / compose-0ep; removes the phone root and all phone XML preference subpages, with TV preferences tracked separately)
  - Crown jewel cross-cutting Lab (this milestone): `application/vlc-android/src/org/videolan/vlc/gui/ComposeInteropLabActivity.kt` + `compose_interop_lab.xml`
  - All leaves + interop + theme: `application/compose/src/main/java/org/videolan/vlc/compose/{components,interop,theme}/*`
  - Richer mocks derived from the Lab: `application/compose/src/main/java/org/videolan/vlc/compose/PreviewUtils.kt`
  - Manifest registration (additive): `application/vlc-android/AndroidManifest.xml`
  - Tracking: bd issue `compose-iju` (labeled `compose-2l4.1.8`)

### The "Every Host Must Have Green Compile Gate Evidence" Policy

**Enforced starting with compose-2l4.1.8**:

> Every interop host (NetworkServerDialog, DebugLogActivity, InfoActivity, ComposeInteropLabActivity, OnboardingWelcomeFragment, and all future ones) **must** have documented evidence that it compiles cleanly against the current `:application:compose` leaves.

**The gate command** (run from the worktree root):
```bash
ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk gradle :application:vlc-android:compileDebugKotlin --no-daemon --console=plain
# or the stricter full suite:
ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain
```

**Where evidence lives**:
- Terminal output captured during the session (SUCCESS blocks).
- Appended to bd notes on the cross-cutting task (`compose-iju`).
- Referenced in the host file headers (see the massive comment blocks in the three hosts).
- Summarized in this README (below) and in future PR descriptions.

**Why this policy**:
- Prevents the hybrid bridge from rotting silently as leaves or tokens evolve.
- The Lab + the two earlier hosts act as canaries.
- Part of the "preview + gate enforcement" acceptance criteria for Wave 1.

**Current gate evidence**:
- 2026-06-04: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin :application:television:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 13s` after replacing the remaining TV detail/browser artwork `AndroidView`/`ImageView` bridges in `VerticalGridActivity`, `DetailsActivity`, `MediaScrapingTvshowDetailsActivity`, and `MediaListActivity` with native Compose `Image` rendering through shared `TvRemoteArtworkImage` and `TvMediaArtworkImage` helpers. This preserves remote poster/backdrop/person image fallbacks, medialibrary `getTvIconRes` fallbacks, content descriptions, and disabled-video-thumbnail behavior. Focused gate `gradle :application:television:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 20s`; a TV-wide focused stale scan found no `AndroidView`, `ImageView`, `loadImage`, `downloadIcon`, or `setImage` image bridge patterns left under `application/television/src/main/java`.
- 2026-06-02: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin :application:television:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 15s` after replacing the phone `SearchActivity` result thumbnail `AndroidView`/`ImageView` bridge with native Compose `Image` rendering backed by `ThumbnailsProvider.obtainBitmap` plus playlist/genre composite artwork loading. This preserves the existing phone search fallback artwork (`ic_no_artist`, `ic_no_album`, `ic_no_thumbnail_1610`, `ic_no_song`) and disabled-video-thumbnail behavior. Focused gate `gradle :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 27s`; focused stale scan found no `AndroidView`, `ImageView`, `loadImage`, `setImage`, or `android.widget` usage left in `SearchActivity.kt`.
- 2026-06-02: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin :application:television:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 11s` after replacing the TV `MainTvActivity` home-card `AndroidView`/`ImageView` artwork bridges with native Compose `Image` rendering backed by `HttpImageLoader` remote poster state and `ThumbnailsProvider.obtainBitmap` media artwork state. This preserves the existing `ic_people_big` and `getTvIconRes` fallbacks plus disabled-video-thumbnail fallback behavior. Focused gate `gradle :application:television:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 28s`; focused stale scan found no `AndroidView`, `ImageView`, `loadImage`, `downloadIcon`, or `setImage` usage left in `MainTvActivity.kt`.
- 2026-06-02: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin :application:television:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 13s` after replacing the TV `MediaScrapingTvActivity` moviepedia result poster `AndroidView`/`ImageView`/`downloadIcon` bridge with native Compose `Image` rendering backed by `HttpImageLoader` remote bitmap state and the existing `ic_video_big` fallback. Focused gate `gradle :application:television:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 24s`; focused stale scan found no `AndroidView`, `ImageView`, `downloadIcon`, or `setImage` usage left in `MediaScrapingTvActivity.kt`.
- 2026-06-02: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin :application:television:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 11s` after replacing the TV `SearchActivity` result thumbnail `AndroidView`/`ImageView` bridge with native Compose `Image` rendering backed by `ThumbnailsProvider.obtainBitmap` and `getTvIconRes` fallbacks. Focused gate `gradle :application:television:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 14s`; focused stale scan found no `AndroidView`, `ImageView`, `loadImage`, or `setImage` usage left in `SearchActivity.kt`.
- 2026-06-02: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin :application:television:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 50s` after replacing the nested landscape `track_info_container` `ConstraintLayout` island with a direct `VLCComposeView` host. The host now renders title/subtitle/detail text, chapter chevrons, seek delay controls, and bookmark seek controls from `AudioPlayer` Compose state, while stale `previous_chapter`, `next_chapter`, `song_title`, `song_subtitle`, and `song_track_info` ids were removed. Focused gate `gradle :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 1m 33s`.
- 2026-06-02: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin :application:television:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 1m` after removing the phone audio-player `player_options_container` `FrameLayout` wrapper and anchoring the `options_background` `VLCComposeView` directly in `AudioPlayerShell`. This preserves click/focus visibility behavior and horizontal-fold bottom-aligned options-panel sizing through `ConstraintLayout.LayoutParams` vertical bias. Focused gate `gradle :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 1m 13s`; stale scans found no `player_options_container`, audio-player `FrameLayout`, `FrameLayout.LayoutParams`, or `Gravity.BOTTOM` usage in the active phone audio-player shell.
- 2026-06-04: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin :application:television:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 25s` after moving `HeaderMediaListActivity` album/playlist tracks from the remaining RecyclerView/PagedListAdapter stack to an activity-owned Compose `LazyColumn`. This deleted the old audio browser adapters, fast scroller, section decoration, swipe/drag helper, and RecyclerView callback interfaces; Compose-native reorder/fast-scroll parity is tracked separately in `compose-co5`.
- 2026-06-02: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin :application:television:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 27s` after moving the phone audio-player and playlist tips overlays from `AudioPlayerTipsHostView`/`AudioPlaylistTipsHostView` plus child `VLCAbstractComposeWidget` wrappers to direct `VLCComposeView` hosts with state controllers. This preserves public `audio_player_tips`/`audio_playlist_tips` IDs, settings flags, tip sequencing, player locking/unlocking, bottom-sheet peek positioning, advanced-options replay, and dismiss/next callbacks. Focused app gate `gradle :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 52s`; focused stale scan found no old tips host/view classes, factory names, or `.tipsView` child access.
- 2026-06-02: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin :application:television:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 42s` after replacing the phone audio-player full-cover `CoverMediaSwitcher`/`AudioMediaSwitcher`/`FlingViewGroup` stack with a direct `VLCComposeView` host backed by `VLCAudioCoverMediaSwitcher` state. This preserves previous/current/next cover pages, artwork/title/subtitle/track-info rendering, chapter button direction, track-info toggling, bottom-sheet lock/unlock during swipes, previous/next swipe callbacks, and no-thumbnail fallback artwork. Focused app gate `gradle :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 1m 37s`; focused stale scan found no active `CoverMediaSwitcher`, `CoverMediaSwitcherItemView`, `AudioMediaSwitcher`, `AudioMediaSwitcherListener`, `FlingViewGroup`, old cover `updateMedia`, or media-switcher listener registration usage in app/compose/TV sources.
- 2026-06-02: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin :application:television:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 17s` after removing the remaining `AndroidView`/`ImageView` bridge from `AudioPlaylistMediaItem` row covers. The phone audio/video playlist row now renders artwork with Compose `Image` from `ThumbnailsProvider.obtainBitmap`, preserves audio/video fallback artwork, and respects disabled video thumbnails. Focused gate `gradle :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 21s`; stale source scan found no `AndroidView`, `ImageView`, `BitmapDrawable`, `loadImage`, `getBitmapFromDrawable`, or `UiTools` usage in `AudioPlaylistMediaItem.kt`.
- 2026-06-02: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin :application:television:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 16s` after moving the phone audio-player playlist search field from `AudioPlaylistSearchFieldView` to a direct `VLCComposeView` under `playlist_search_text`, with `AudioPlayer` now owning query and focus state. Focused gate `gradle :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 32s`; stale source scans found no `AudioPlaylistSearchFieldView`, `requestSearchFocus`, `clearSearchText`, or `playlistSearchText.onQueryChanged` references.
- 2026-06-02: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin :application:television:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 38s` after replacing the phone audio-player collapsed `HeaderMediaSwitcher` pager with a direct `VLCComposeView` host backed by `VLCAudioHeaderMediaSwitcher` state. This preserves previous/current/next header pages, cover/title/artist rendering, chapter title handling, TalkBack content description, tap slide toggle, long-press copy/resume-video behavior, and previous/next swipe callbacks. Focused app gate `gradle :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 26s`; focused phone audio-player scan found no active `org.videolan.vlc.gui.view.HeaderMediaSwitcher`, `HeaderMediaSwitcherItemView`, old header `updateMedia`, or header listener registration usage, with the remaining media switcher View path limited to `CoverMediaSwitcher`.
- 2026-06-02: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin :application:television:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 30s` after replacing the phone audio-player `AbRepeatControlsView` and `AbRepeatMarkerContainerView` wrappers with direct `VLCComposeView` hosts backed by `AudioPlayer` Compose state. This preserves A-B repeat marker fractions, show/hide behavior, add-marker click behavior, reset/stop helper visibility, and video HUD wrapper compatibility. Focused app gate `gradle :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 22s`; focused phone audio-player scan found no `AbRepeatControlsView`, `AbRepeatMarkerContainerView`, `setMarkerPositions`, or wrapper `setMarkerText` usage in the active audio-player surface.
- 2026-06-02: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin :application:television:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 30s` after replacing the phone audio-player `PlayerOptionsPanelView` and `BookmarksPanelView` wrappers with direct `VLCComposeView` hosts backed by `AudioPlayer` Compose state and the shared `PlayerOptionsPanelHost`/`BookmarkPanelHost` delegates. This preserves options generation/dismiss/action callbacks, option icon updates, bookmark callbacks, jump delay text/descriptions, add-focus/accessibility, marker visibility handoff, and horizontal foldable options-panel sizing. Focused app gate `gradle :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 19s`; focused phone audio-player scan found no `PlayerOptionsPanelView` or `BookmarksPanelView` usage in the active audio-player surface.
- 2026-06-02: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin :application:television:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 19s` after moving the phone audio-player bookmark marker strip from `BookmarkMarkerContainerView` to a direct `VLCComposeView` host backed by `VLCBookmarkMarkers` state and `BookmarkMarkerHost`. This preserves bookmark fraction updates, show/hide behavior, bookmark-panel visibility handoff, and video HUD `BookmarkMarkerContainerView` compatibility. Focused app gate `gradle :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 29s`; focused phone audio-player scan found no `BookmarkMarkerContainerView` usage in the active audio-player surface.
- 2026-06-02: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin :application:television:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 41s` after moving the phone audio-player collapsed mini progress bar and full-player timeline from view-specific Compose wrappers to direct `VLCComposeView` hosts driven by `AudioPlayer` Compose state. This deletes `AudioMiniProgressBarView`, removes the phone-only `AudioTimelineSeekBarView` subclass, preserves mini/full progress max/progress updates, long-press seek preview, drag seek callbacks, TalkBack timeline text, and keeps the shared `PlayerTimelineSeekBarView` only for the video HUD bridge. Focused stale scans found no `AudioMiniProgressBarView`, `AudioTimelineSeekBarView`, or imperative `binding.timeline`/`binding.progressBar` max/progress writes in the active audio-player path.
- 2026-06-02: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin :application:television:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 16s` after replacing the TV audio-player root `AndroidView`/programmatic `ConstraintLayout`/child `ComposeView` anchor shell with a single pure Compose `TvAudioPlayerScreen`, preserving the right playlist rail, left artwork/title column, quick actions, bottom progress/timeline/bookmark markers/transport controls, bookmark/options overlays, focus gating state, and playback callbacks. Focused gate `gradle :application:television:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 22s` after fixing a Compose `padding` argument mismatch; focused stale scan found no `AndroidView`, `ConstraintLayout`, or child `ComposeView` anchors in the TV audio-player package.
- 2026-06-02: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin :application:television:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 10s` after replacing the TV audio-player `PlayerOptionsPanelView` wrapper with native Compose `TvAudioPlayerOptionsPanel` state and shared `VLCPlayerOptionsPanelContent`, preserving option generation, dismiss/option callbacks, option icon updates, initial focus, bookmark option handoff, and back-dismiss behavior. Focused gate `gradle :application:television:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 26s` after fixing an override default-argument compile error; focused stale scan found no `PlayerOptionsPanelView` usage in the TV audio-player package.
- 2026-06-02: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin :application:television:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 9s` after replacing the TV audio-player `BookmarksPanelView` wrapper with native Compose `TvAudioBookmarksPanel` state and shared `VLCBookmarksPanelContent`, preserving bookmark list rows, empty state, seek controls, jump delay labels, add focus/accessibility, rename/delete actions, marker visibility, and playlist/quick-action focus gating. Focused gate `gradle :application:television:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 27s`; focused stale scan found no `BookmarksPanelView` usage in the TV audio-player package.
- 2026-06-02: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin :application:television:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 1m 28s` after replacing the TV audio-player title/artist and elapsed/length `TextView`s with native Compose `TvAudioTrackInfo` and `TvAudioProgressLabels`, preserving the `media_title` and `media_time` constraint anchors used by the remaining programmatic shell. Focused gate `gradle :application:television:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 3m 53s` after fixing nullable PlayerState title/artist normalization.
- 2026-06-02: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin :application:television:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 1m 21s` after replacing the TV audio-player shuffle/previous/play-pause/next/repeat/more `AppCompatImageView` transport row with native Compose `TvAudioTransportControls`, preserving playback callbacks, shuffle/repeat icon state, active tinting, accessibility labels, and the `button_play` host anchor used by the remaining programmatic shell. Focused gate `gradle :application:television:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 2m 40s`.
- 2026-06-02: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin :application:television:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 1m 26s` after replacing the TV audio-player playback-speed/sleep `LinearLayout` quick-action chips with native Compose `TvAudioQuickActions`, preserving speed/global-rate icon selection, click/long-click dialog/reset behavior, sleep timer clearing, bookmark focus gating, and empty-row focus removal. Focused gate `gradle :application:television:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 2m 3s`.
- 2026-06-02: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin :application:television:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 4m 7s` after replacing the TV audio-player `PlaylistAdapter`/RecyclerView playlist with a native Compose `TvAudioPlaylist` LazyColumn, preserving row cover loading, current-playing bars, item selection/playback, focus gating while bookmarks are shown, and selected-item scroll behavior.
- 2026-06-02: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:app:compileDebugAndroidTestKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 23s` after repairing AndroidTest sources that still targeted removed XML-era IDs/helpers (`audio_list`, `network_list`, `video_grid`, context sheet rows, playlist dialog IDs, and `SavePlaylistDialog` test injection hooks). The same pass removed the obsolete moviepedia `kotlin-kapt` plugin so the Compose-only `:application:moviepedia` module no longer regenerates stale DataBinding Java sources; the follow-up module gate `gradle :application:compose:build :application:vlc-android:compileDebugKotlin :application:moviepedia:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 8s`.
- 2026-06-02: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin :application:moviepedia:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 13s` after moving `MediaScrapingActivity` to a Compose-hosted screen, enabling Compose in `:application:moviepedia`, deleting `moviepedia_activity.xml`, `moviepedia_item.xml`, and `MediaScrapingResultAdapter.kt`, and removing moviepedia DataBinding generation.
- 2026-06-02: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin :application:remote-access-server:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 10s` after moving `RemoteAccessShareActivity` to a Compose-hosted screen and deleting `remote_access_share_activity.xml`, `remote_access_connection_item.xml`, `remote_access_share.xml`, and `ConnnectionAdapter.kt`. The same pass added a debug/test-only app minSdk floor of 21 so AndroidTest builds no longer fail at the Compose Material3 manifest merge. `:application:app:compileDebugAndroidTestKotlin` now reaches Kotlin test-source compilation; remaining stale XML-era test IDs/helpers are tracked by bd `compose-cop`.
- 2026-06-02: Widget RemoteViews boundary documented for bd compose-l0l. Fresh inventory found the remaining active widget runtime XML files are `widget_pill.xml`, `widget_mini.xml`, `widget_mini_initial.xml`, `widget_micro.xml`, `widget_macro.xml`, `widget_content_full_player.xml`, and `widget_old.xml`, with provider metadata in `widget_provider_mini.xml`, `widget_provider_b.xml`, and `widget_provider_w.xml`. The widget configuration/migration screens are already Compose-hosted, while the launcher-hosted App Widget runtime remains explicitly RemoteViews-scoped and is not counted as an Activity/screen Fragment blocker. A separate Glance prototype task tracks any future Compose-style RemoteViews rewrite.
- 2026-06-02: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 45s` after renaming the residual phone Compose route/container contracts away from Fragment terminology. `fragment_placeholder` is now `content_placeholder`, `KEY_FRAGMENT` is now `KEY_DESTINATION`, the secondary route extra is now `destination`, and the remaining `fragment_option_*` menu resources are renamed; focused active-source/resource scans found no Fragment API or Fragment terminology matches in app, app-test, or TV sources.
- 2026-06-02: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 25s` after replacing the portrait and landscape audio-player `ConstraintLayout` shell resources with `AudioPlayerShell.kt`. The programmatic shell preserves the header and cover media switchers, timeline and marker containers, options/bookmarks panels, foldable hinge controls, landscape track-info nodes, and stable IDs through `ids.xml`; focused scans found no active audio-player shell layout callers and only widget RemoteViews layout XML remains.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 11s` after replacing the final video player root XML shells (`player.xml` and `player_remote_control.xml`) with `VideoPlayerShell.kt`. The programmatic shell preserves the VLC video layout, overlay hosts, playlist/search/options hosts, secondary-display overlay background, foldable hinge layout params, and stable IDs through `ids.xml`; focused scans found no active `R.layout.player`, `R.layout.player_remote_control`, `@layout/player`, or `@layout/player_remote_control` references.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 12s` after replacing the secondary-display `player_remote_control.xml` video playlist `RecyclerView` host with the shared `VLCComposeView` / `AudioPlaylistQueue` contract already used by the primary player shell. Focused scans found no `RecyclerView` playlist host remaining in the audio/video player shell XML files.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 1m 7s` after replacing `HeaderMediaSwitcher` and `CoverMediaSwitcher` item XML inflation with Compose-backed `HeaderMediaSwitcherItemView` / `CoverMediaSwitcherItemView` pages. Deleted `audio_media_switcher_item.xml` plus all `cover_media_switcher_item.xml` layout variants; focused scans found no active references to those layouts.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 1m 5s` after replacing the generated `AudioPlayerBinding` view holder with `AudioPlayerViews` and converting `AudioPlayerShell.kt` plus `AudioPlayerShell.kt landscape branch` from DataBinding `<layout>` wrappers to plain `ConstraintLayout` roots. Focused active-code scans found no `AudioPlayerBinding`, DataBinding tags/expressions, `binding.fragment`, or `binding.canShuffle` in the audio-player path.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 34s` after replacing the final `player_hud.xml` XML shell with direct `VideoHudOverlayView` hosts in `player.xml` and `player_remote_control.xml`. The new root creates the same stable HUD IDs and Compose-backed leaf controls programmatically; `ids.xml` now owns the stable IDs formerly declared only by the deleted layout. Focused active-code scans found no `@layout/player_hud`, `R.layout.player_hud`, `player_hud_stub`, `ViewStubCompat`, `PlayerHudBinding`, DataBinding tags/expressions, or active `player_hud.xml` file.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 1m` after replacing the generated `PlayerHudBinding` view holder with `VideoHudOverlayViews` and converting `player_hud.xml` from a DataBinding `<layout>` wrapper to a plain `ConstraintLayout` root. Focused active-code scans found no `PlayerHudBinding`, `DataBindingUtil`, DataBinding tags/expressions, `player_hud_stub`, `ViewStubCompat`, or orphan video HUD binding adapters in the active video HUD path.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 59s` after replacing the remaining `player_hud_stub` `ViewStubCompat` hosts in `player.xml` and `player_remote_control.xml` with direct `player_hud` includes and moving `player_hud.xml` progress/click DataBinding expressions into explicit `VideoPlayerOverlayDelegate` observers/listeners. Focused scans found no `player_hud_stub`, `ViewStubCompat`, `<data>`, `@{}`, or orphan video HUD binding adapters in the active video HUD path.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 21s` after replacing the video HUD stats panel XML internals with `VideoStatsOverlayView`, preserving media stat sampling, track rows, graph/legend rendering, close action, scroll callbacks, and portrait/landscape layout switching while removing the old `NestedScrollView`, dynamic `info_grids`, `PlotView`, `LegendView`, and `stats_close` binding path.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 26s` after replacing the video HUD play/pause `ImageView` with `VideoHudIconButtonView`, preserving the existing `player_overlay_play` ID, click/focus/visibility/enabled/content-description contract, play/pause/disabled icon updates, and removing the video-only animated-vector setup from `VideoPlayerActivity`/`VideoPlayerOverlayDelegate`.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 24s` after replacing the video HUD orientation `ImageView` with `VideoHudIconButtonView` and the rewind/forward jump-number `TextView`s with `VideoHudSeekJumpLabelView`, preserving dynamic orientation resource updates, click/long-click dispatch, seek label text/visibility updates, and accessibility suppression.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 23s` after replacing the static video HUD close/tracks/previous/rewind/forward/next/resize/more `ImageView` buttons with `VideoHudIconButtonView`, preserving existing IDs, click and long-click dispatch, enabled/icon updates, key listeners, and layout constraints while leaving only the dynamic orientation and animated play/pause `ImageView`s in that control row.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 7s` after replacing the video HUD A-B repeat reset/stop/warning `ImageView` buttons with `VideoHudIconButtonView`, preserving the existing IDs, content descriptions, click listeners, visibility hooks, and icon resources.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 21s` after replacing the video HUD elapsed/length `FocusableTextView` labels with `VideoTimelineTimeLabelView`, preserving the existing IDs, click-to-toggle remaining time behavior, focus highlight, lock enable state, margins, and bookmark height anchoring.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 30s` after replacing the video HUD `AccessibleSeekBar` with `VideoTimelineSeekBarView`, sharing the Compose timeline bridge with the audio player, preserving max/progress binding, seek callbacks, TalkBack announcements, and deleting the obsolete native seekbar class.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 17s` after replacing `swipe_to_unlock.xml` with the Compose-backed `SwipeToUnlockView`, preserving touch drag, RTL handling, DPAD long-press unlock, focus handoff, and deleting the obsolete rounded background XML resource.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 16s` after replacing the video top/right HUD `ViewStub`/DataBinding payload with `VideoHudRightOverlayView`, preserving title/warning display, nav/screenshot/renderer/secondary-display/playlist actions, TV clock visibility, and quick-action chip state while deleting `player_hud_right.xml` and the obsolete quick-action XML bridge.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 15s` after replacing the video double-tap seek and tap-and-hold fast-play `ViewStub` payload with `VideoSeekOverlayView`, preserving direction text, arrow animation, TV-shaped seek panels, fast-play title/indicator, and deleting `player_overlay_seek.xml` plus the orphan `HalfCircleView`.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 15s` after replacing the video audio/subtitle delay settings `ViewStub` payload with `VideoDelayOverlayView`, preserving delay value updates, marker state, reset/apply/close actions, focus handoff, and deleting `player_overlay_settings.xml`.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 27s` after replacing the phone shell `scan_progress.xml` ViewStub payload with `ScanProgressView`, preserving media-library discovery/progress updates and CoordinatorLayout anchoring while deleting the obsolete scan background XML resources.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 22s` after replacing the video brightness/volume `ViewStub` payloads with `VideoVerticalProgressOverlayView`, preserving value updates, fade-out handling, and audio-boost double-range rendering while deleting `player_overlay_brightness.xml` and `player_overlay_volume.xml`.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 23s` after replacing the video player tips `ViewStub`/`player_tips.xml` overlay with `VideoTipsHostView`, preserving dismiss/next/control selection sequencing through `VideoTipsDelegate`, and hosting the Compose overlay directly in `player.xml` and `player_remote_control.xml`.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin :application:television:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 10s` after replacing the TV audio-player playlist DataBinding row with a ComposeView-backed `TvPlaylistRow`, deleting `tv_playlist_item.xml`, preserving play/pause row refresh, and direct-hosting the TV bookmark/player-options Compose panels instead of stale stub layout refs.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 26s` after replacing the video HUD quick-action `HorizontalScrollView`/Material chip strip with `VLCVideoQuickActions`, and deleting the audio playlist tips XML payload in favor of a Compose tips host. The later 2026-06-02 pass moved the audio-player and playlist tips overlays from custom host/widget View wrappers to direct `VLCComposeView` hosts with state controllers.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 23s` after adding `VLCEmptyState`, hosting it directly in the phone audio, video, browser, and playlist Compose screens, adding Lab/PreviewUtils coverage, and deleting the unused legacy empty-loading XML view/styleable.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 11s` after routing the phone Advanced and Optional Features preference subpages through `PreferencesComposeSubpages.kt`, preserving DB clear/dump/export/restore actions, network caching validation, LibVLC restart side effects, nightly install/update/debug actions, and feature-flag warning flow.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 9s` after routing the phone Subtitles preference subpage through `PreferencesComposeSubpages.kt`, preserving preset reset behavior, text encoding/preferred-language lists, dependent background/shadow/outline rows, color and opacity controls, and deferred media-pipeline restart on page exit.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 9s` after routing the phone Interface preference subpage through `PreferencesComposeSubpages.kt`, preserving theme default migration, locale/theme restart prompts, list-title/include-missing restart state, sleep timer summary/dialog refresh, media-seen result propagation, incognito dependency, and phone visibility rules.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 9s` after routing the phone Audio preference subpage through `PreferencesComposeSubpages.kt`, preserving preferred-language locale entries, `PreferenceVisibilityManager`-gated rows, digital-output summary, headset detection callback, ReplayGain dependencies/formatting/restart behavior, and SoundFont picker.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 12s` after routing the phone Video preference subpage through `PreferencesComposeSubpages.kt`, preserving the video-settings disclaimer, preferred-resolution restart, popup permission checks, and secondary-display rows.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 12s` after routing the phone Remote Access preference subpage through `PreferencesComposeSubpages.kt`, including its onboarding menu, server status action, server start/stop, restart trigger, and media-library multi-select behavior.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 11s` after routing Casting, Parental Control, and Android Auto phone preference subpages through `PreferencesComposeSubpages.kt`.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 9s` after replacing the audio-player XML shell with a plain `FrameLayout` host and constructing `AudioPlayer` as a bottom-sheet controller from `AudioPlayerContainerActivity`.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 20s` after replacing the phone root preference XML screen with `PreferencesRootScreen` hosted by `PreferencesActivity`, preserving root setting toggles/list dialogs and endpoint routing into existing subpages.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 22s` after moving active album track rows to `ComposeView` + `VLCBrowserItemRow`, preserving track-number/current-track/subtitle/action behavior. The later full header-media cutover replaced this adapter bridge with the current direct Compose `LazyColumn`.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 17s` after deleting stale browser/simple row XML shims and unused generated-binding fallback holders for browser row/card, album track, MRL panel, search button, simple row, and audio RecyclerView shell. The only remaining similarly named browser artifact is the live `audio_browser_item_size` dimension used by Compose-backed rows.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 48s` after replacing the album-track current-row playback indicator with native Compose animated bars and deleting the old custom View/XML bridge. Focused scans for the old class, layout, and child-id symbols returned no matches.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin :application:television:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 35s` after replacing the fast-scroll custom view's inflated layout with direct child construction, deleting two orphan layout leftovers, and migrating the TV audio timeline to the shared Compose-backed slider bridge. Focused active scans found no removed layout references or TV native seekbar resource references.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 21s` after replacing the legacy popup video overlay XML inflation with direct `PopupLayout` child construction. The custom popup still owns its gesture/window behavior while the old layout file is gone; the remaining app/TV layout XML count is 11.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 9s` after deleting `playlist_item.xml` and `PlaylistItemView.kt`, moving the app-side row artwork/action implementation into `AudioPlaylistMediaItem.kt`, and creating the audio playlist tips fake rows programmatically as Compose widgets. The later 2026-06-02 pass removed the residual row-cover `AndroidView` bridge.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk gradle :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 22s` after deleting `playlist_item.xml` and `PlaylistItemView.kt`, moving the app-side row artwork/action implementation into `AudioPlaylistMediaItem.kt`, and creating the audio playlist tips fake rows programmatically as Compose widgets. The later 2026-06-02 pass removed the residual row-cover `AndroidView` bridge.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 20s` after replacing the video-player overlay `video_playlist` RecyclerView host with the shared VLCComposeView-backed `AudioPlaylistQueue` LazyColumn, moving `VideoPlayerActivity`/`VideoPlayerOverlayDelegate` off the former shared `PlaylistAdapter` path and deleting `PlaylistAdapter.kt`.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 17s` after replacing the audio-player `songs_list` RecyclerView host with a VLCComposeView-backed `AudioPlaylistQueue` LazyColumn, moving AudioPlayer off `PlaylistAdapter`/`ItemTouchHelper` while preserving playlist row actions, context menu, stop-after-this, swipe removal, and inline tablet move/delete controls in Compose.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 14s` after replacing `playlist_item.xml` DataBinding content with `PlaylistItemView` + `VLCAudioPlaylistItem`, including the audio playlist tips fake rows, while keeping the existing RecyclerView swipe/drag host for the next list-surface migration.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 17s` after replacing the shared bookmark panel toolbar/RecyclerView/empty state/bottom controls, `bookmark_item.xml` DataBinding rows, and the bookmark overflow menu XML with `BookmarksPanelView` + `VLCBookmarkRow`, with marker container callbacks preserved.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 18s` after replacing the shared player options `BrowseFrameLayout`/`RecyclerView` shell and row bridge with `PlayerOptionsPanelView`, preserving dynamic option callbacks.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 23s` after replacing `player_option_item.xml` DataBinding rows with `PlayerOptionItemView` + `VLCPlayerOptionItem` in `PlayerOptionsDelegate`, preserving the existing options panel RecyclerView/focus shell.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 19s` after replacing the shared A-B repeat include root `ConstraintLayout`/child bridge views with `AbRepeatControlsView` + `VLCAbRepeatControls`, with audio/video hosts driving the include root directly.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 22s` after replacing the audio-player restore-video snackbar hint with `VLCAudioResumeVideoHint` hosted under the existing audio player surface via `resume_video_hint` in portrait and landscape.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 22s` after initially replacing the shared audio/video bookmark marker overlay `ConstraintLayout` + dynamic `ImageView` marker population with `VLCBookmarkMarkers` through `BookmarkMarkerContainerView` under the existing `bookmark_marker_container` IDs. The later 2026-06-02 pass moved the phone audio-player marker strip to a direct Compose host while keeping the video HUD bridge.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 21s` after replacing the audio-player blurred cover background `ImageView` with `VLCAudioPlayerBackground` through an XML-friendly Compose widget under the existing `backgroundView` ID. The later 2026-06-02 pass moved this from `AudioPlayerBackgroundView` to a direct `VLCComposeView` host driven by AudioPlayer state.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 9s` after replacing the audio-player full-player timeline `AccessibleSeekBar` with `VLCAudioTimelineSlider` through an XML-friendly Compose widget under the existing `timeline` ID.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 21s` after replacing the audio-player playlist search `TextInputLayout`/`EditText` with `VLCAudioPlaylistSearchField` through an XML-friendly Compose widget under the existing `playlist_search_text` ID. The later 2026-06-02 pass moved this from `AudioPlaylistSearchFieldView` to a direct `VLCComposeView` host driven by AudioPlayer query and focus state.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 25s` after replacing the audio-player top/bottom gradient overlay `View`s with `VLCAudioPlayerGradient` through XML-friendly Compose widgets under the existing `top_gradient` and `bottom_gradient` IDs. The later 2026-06-02 pass moved these from `AudioPlayerTopGradientView`/`AudioPlayerBottomGradientView` to direct `VLCComposeView` hosts.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 21s` after initially replacing the audio-player collapsed mini `ProgressBar` with `VLCAudioMiniProgressBar`, preserving the existing `progressBar` max/progress and slide-height contract. The later 2026-06-02 pass moved this from a wrapper to a direct Compose host.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 21s` after replacing the audio-player collapsed header background/divider XML Views with `VLCAudioHeaderBackground` / `VLCAudioHeaderDivider` under the existing animated IDs.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 13s` after replacing the shared A-B repeat add-marker button with `VLCAbRepeatAddMarkerButton` via the shared Compose root, with audio/video hosts driven by the callback-based `manageAbRepeatStep` overload.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 21s` after replacing the shared A-B repeat add-marker chip icon with `VLCAbRepeatChipIcon` via the shared Compose root.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 22s` after replacing the audio-player elapsed/length timeline labels with `VLCAudioTimelineTimeLabel`.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 20s` after replacing the landscape audio-player track info title/subtitle/detail text with `VLCAudioTrackInfoText`.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 20s` after initially replacing the audio-player A-B repeat timeline marker leaves with `VLCAudioAbRepeatMarker`.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 35s` after replacing the audio/video A-B repeat marker guideline/DataBinding islands with `AbRepeatMarkerContainerView` + `VLCAudioAbRepeatMarkers`.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 35s` after replacing the transient video info `ViewStub`/`player_overlay_info.xml` overlay with `VideoInfoOverlayView`.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 24s` after replacing the remaining audio/video player options and bookmarks stub wrappers with direct `PlayerOptionsPanelView` and `BookmarksPanelView` hosts, deleting the two wrapper layouts.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk ANDROID_SDK_ROOT=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 24s` after removing the remaining `AudioPlayerShell.kt` / `AudioPlayerShell.kt landscape branch` DataBinding variables and XML expressions, with shuffle visibility now driven from `AudioPlayer`.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 21s` after replacing the audio-player foldable hinge left/right affordances with `VLCAudioHeaderTransportButton`.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 21s` after replacing the landscape audio-player chapter chevrons with `VLCAudioHeaderTransportButton`.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 21s` after replacing the audio-player AB-repeat reset/stop header actions with `VLCAudioHeaderActionButton`.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 21s` after replacing the audio-player cover-mode seek/bookmark HUD buttons and delay labels with `VLCAudioSeekHudButton` / `VLCAudioSeekDelayLabel`.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 20s` after replacing the full-player bottom audio transport controls with `VLCAudioHeaderTransportButton`.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 21s` after replacing the tablet audio-player header transport strip with `VLCAudioHeaderTransportButton`.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 21s` after replacing the collapsed audio-player mini play/pause button with `VLCAudioHeaderPlayPauseButton`.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 23s` after replacing the collapsed audio-player search, playlist switch, and overflow action icons with `VLCAudioHeaderActionButton`.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 21s` after replacing the collapsed audio-player header time label with `VLCAudioHeaderTimeLabel`.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 25s` after replacing the audio queue progress pill with `VLCAudioQueueProgressPill`.
- 2026-06-01: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 10s` after replacing the audio-player quick-action ChipGroup with `VLCAudioPlayerChips`.
- 2026-05-31: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk gradle :application:vlc-android:compileDebugKotlin --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 1m 24s` after the worktree was made tolerant of absent local `libvlcjni/libvlc` by falling back to the published `org.videolan.android:libvlc-all` artifact.
- 2026-05-31: `ANDROID_HOME=/Users/bernardoferrari/Library/Android/sdk gradle :application:compose:build --no-daemon --console=plain` completed with `BUILD SUCCESSFUL in 1m 37s`.
- All current Wave 0/1 hosts (NetworkServerDialog, DebugLogActivity, InfoActivity, ComposeInteropLabActivity, OnboardingWelcomeFragment, OTPCodeActivity) participate in the app Kotlin gate.

Future agents: re-run the gate after touching any leaf or host, append the SUCCESS tail + timestamp to bd, and update this section.

---

**Part of the phase-0-compose-bootstrap effort.**  
See parent tracking issue and reconnaissance notes for the overall 80% migration strategy.

<?xml version="1.0" encoding="utf-8"?>
<!--
  App shell manifest. Cleartext policy: @xml/network_security_config (compose-knn1).
  requestLegacyExternalStorage residual: still required for broad file:// /
  secondary-storage media library scanning on API 29 until full scoped-storage /
  SAF / MediaStore migration (Permissions / StoragePermissionsDelegate).
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
        android:installLocation="auto">

    <application
        android:name=".VLCApplication"
        android:allowBackup="true"
        android:appCategory="audio"
        android:banner="@drawable/banner"
        android:fullBackupContent="@xml/backup_rules"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:logo="@drawable/icon"
        android:networkSecurityConfig="@xml/network_security_config"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.VLC.Transparent"
        tools:targetApi="q" />
</manifest>
